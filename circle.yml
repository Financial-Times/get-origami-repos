
machine:
  node:
    version: 8

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=${NPM_AUTH_TOKEN}" > ${HOME}/.npmrc

test:
  override:
    - make ci

deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - make npm-publish
